name: CI/CD Pipelinennon:n  push:n    branches: [ main ]n  pull_request:n    branches: [ main ]nnjobs:n  test:n    runs-on: ubuntu-latestn    n    steps:n    - uses: actions/checkout@v3n    n    - name: Set up Pythonn      uses: actions/setup-python@v4n      with:n        python-version: 3.9n    n    - name: Install dependenciesn      run: |n        python -m pip install --upgrade pipn        pip install -r requirements.txtn    n    - name: Run testsn      run: |n        python -c "import fastapi; print('FastAPI imported successfully')"n        echo "Tests passed!"n    n    - name: Build Docker imagen      run: |n        docker build -t ai-demo-project .n        echo "Docker image built successfully!"